This folder is reserved for React Native bundled assets.

The original web app used `figma:asset/...` imports, which are not supported in Expo/React Native.
Those imports were replaced with native-compatible sources in `src/assets/logo.ts` and native UI badge components.
